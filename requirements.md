你是一个资深的 Python 全栈开发专家和前端排版专家。请帮我开发一个“带拼音田字格的语文词语默写纸生成器”的 Web 应用。

**一、 核心需求**
用户在前端文本框中输入一组汉字词语，系统自动将其转换为标准的“拼音 + 田字格”排版页面，并支持直接调用浏览器打印成 A4 纸供小学生练习。

**二、 技术栈要求**
* **后端：** Python 3.11+、FastAPI（轻量高效）、`pypinyin` 库（用于汉字转拼音）。
* **前端：** 原生 HTML5 + CSS3 + 少量 Vanilla JS。不需要复杂的构建工具（如 Vue/React），保持单文件或简单的结构以便于维护。
* **部署：** 请提供一份简单的 `Dockerfile` 和 `docker-compose.yml`，将服务容器化。

**三、 具体功能与实现细节**

1.  **输入与分词处理 (Backend)**
    * 接收前端传来的字符串，使用正则表达式处理分隔符。支持的分隔符包括：全角空格、半角空格、多个连续空格、中文逗号（，）、英文逗号（,）、制表符（\t）以及换行符（\n）。
    * 将字符串按分隔符切分，过滤掉空字符。

2.  **拼音转换 (Backend)**
    * 使用 `pypinyin` 进行转换。对于多音字，尽量根据词语上下文进行智能匹配（启用 `pypinyin` 的词组模式）。
    * 需要返回包含“原词”和“对应拼音”的数据结构给前端。

3.  **田字格渲染与排版 (Frontend)**
    * **田字格绘制：** 绝对不能使用低分辨率图片。请使用 CSS 的 `background-image` (结合 `linear-gradient` 绘制虚线十字) 或者内联 SVG 来绘制标准的田字格，确保在 300dpi 打印机下边缘锐利不失真。田字格线条采用浅灰色，既能让小朋友能识别出这是田字格，也不能过浓导致掩盖了小朋友后续的铅笔字。
    * **布局：** 
        * 每个字占一个田字格，上方保留对应的拼音四线三格区域（默认留白，如果是带拼音版本则填入拼音）。
        * 词语之间要有适当的间距区分。
    * **页面默认占位符：** 前端输入框的 `placeholder` 请设置为：“例如：方序 语文 默写，支持空格或逗号分隔”。

4.  **打印优化 (@media print)**
    * 适配 A4 纸张尺寸（宽 210mm，高 297mm）。
    * 打印时，自动隐藏输入框、提交按钮等交互元素。
    * 使用 `page-break-inside: avoid;` 防止同一个词语或同一个田字格被跨页截断。
    * 强制打印背景色（`-webkit-print-color-adjust: exact;`），确保田字格的虚线和边框能正常打印。

**四、 执行步骤**
请按以下顺序输出代码：
1. 先写出后端的 `main.py` 和 `requirements.txt`。
2. 再提供前端的 `templates/index.html`（包含 CSS 和 JS）。
3. 最后提供 `Dockerfile` 和 `docker-compose.yml`。
确保代码可以直接运行，无需我进行二次大改。